# 생의 무늬 — 가중치·관계 모듈

엔진 본체는 VPS `/opt/data/saengui-munui/backend/` 에 있다.
여기에는 **판단이 박혀 있는 모듈만** 둔다 — 가장 많이 바뀌고, 가장 조용히 틀어지는 것들이다.

```
saengui/
├── weights.py                 ← VPS engines/weights.py 로 배치
├── test_weights.py            ← 배치 후 VPS에서 실행 (17/17)
├── relation_engine.py         ← VPS engines/relation_engine.py 로 배치
├── test_relation_engine.py    ← 배치 후 VPS에서 실행 (29/29)
└── README.md
```

---

## 바뀐 것

**이전** — 한 표에 성격이 다른 것이 섞여 있었다.

```
행동 45% + 맥락 30% + MBTI 20% + 사주 5%
```

**이후** — 표를 둘로 나눴다.

### ① 증거 축 (합 100) — 무엇이 사실인가

| 축 | 비중 | 변화 |
|---|---:|---|
| 행동 — 실제로 한 것·기록한 것 | 40% | 45 → 40 |
| **관계 — 오행 교환(생·극·합·충)** | **25%** | **신설** |
| 맥락 — 지금의 상황·감정·관심 | 25% | 30 → 25 |
| 타고난 지표 — 사주 5 + 손금 5 | 10% | 5 → 10 |

### ② 형식 층 (가중치 없음) — 어떻게 말할 것인가

| 층 | 역할 |
|---|---|
| 주역 64괘 | 리포트의 서사 골격 |
| MBTI | 어휘와 톤 |
| 손금 오버레이 | 물증·개인화 지각(신뢰) |

**MBTI와 주역은 근거가 아니라 말하는 방식이다.** "무엇이 사실인가"에 기여하지 않는 것에
가중치를 주면 비중 싸움이 되고, "주역이 몇 퍼센트 맞았나"라는 답할 수 없는 질문이 생긴다.

형식 층으로 내려간다고 역할이 줄지 않는다. 오히려 커진다 —
주역은 모든 리포트의 뼈대가 되고, MBTI는 모든 문장의 톤이 된다.

---

## 코드가 스스로 지키는 것

숫자만 옮기면 몇 달 뒤 조용히 되돌아간다. 그래서 판단을 코드에 박았다.

| 장치 | 하는 일 |
|---|---|
| `_validate()` (import 시 실행) | 합이 100이 아니면 **앱이 안 뜬다** |
| `FORBIDDEN_IN_EVIDENCE` | `MBTI`·`주역`·`64괘` 를 증거 축에 넣으면 **터진다** |
| `resolve(available)` | 데이터 없는 축을 재정규화하되 **빠진 축을 함께 돌려준다** |
| `prompt_block()` | 프롬프트에 숫자를 손으로 적지 않게 한다 |
| `cache_key()` | 표를 고치면 캐시가 무효화된다 |

`resolve()`가 빠진 축을 돌려주는 이유: 관계 축은 신설이라 초기 사용자에게 데이터가 없다.
25%를 그냥 없애고 나머지를 늘리면 **왜 그 해석이 나왔는지 나중에 설명할 수 없다.**
빠진 것은 빠졌다고 리포트 근거란에 남긴다.

---

## VPS 이관 절차

```bash
# 1. 배치
#    저장소 saengui/weights.py → /opt/data/saengui-munui/backend/engines/weights.py

# 2. 검증 — 반드시 VPS에서 실행한다
cd /opt/data/saengui-munui/backend/engines
python3 test_weights.py        # 17/17 통과해야 한다. 하나라도 실패하면 멈춘다.

# 3. llm_writer.py 연결
#    기존에 하드코딩된 45/30/20/5 를 찾아 지우고 아래로 바꾼다
```

```python
from weights import prompt_block, cache_key

# 사용 가능한 축 이름을 넘긴다. 데이터가 없는 축은 넣지 않는다.
available = {"행동", "맥락", "타고난지표"}   # 관계 데이터가 생기면 "관계" 추가
block = prompt_block(available)             # 프롬프트에 이 문자열을 그대로 끼운다
```

### 주의

- **`llm_writer.py` 는 아직 확인하지 못했다.** 현재 어떤 형태로 가중치를 쓰는지 모른다.
  파일을 열어 실제 코드를 보고 연결하되, **숫자를 두 곳에 적지 마라.**
  두 곳에 적힌 숫자는 반드시 갈라진다.
- 하드코딩된 값을 지웠는지 확인한다: `grep -n "45\|20\|MBTI" llm_writer.py`
- `quality_gate.py` Q09(자기선택 MBTI 구분)는 그대로 둔다. 이 변경과 충돌하지 않는다.
- 캐시를 쓰고 있다면 `cache_key()` 를 키에 포함시킨다.
  표를 고쳐놓고 옛 리포트가 계속 나오는 것이 가장 찾기 어려운 실패다.

---

## 관계 축을 언제 켜는가

`관계` 축(25%)을 채울 `relation_engine.py` 는 아래에 만들었다.
다만 5단계 중 **대운 교차**는 `saju_engine` 의 대운 산출을 `{연도: 오행}` 으로
바꾸는 어댑터가 있어야 돈다. 그 전까지는 `daeun=None` 으로 두고 그 단계를 비운다.

관계 데이터가 아직 없는 사용자에게는 `resolve()` 가 관계 축을 빼고 재정규화하며,
빠졌다는 사실을 함께 돌려준다. **그 상태로 동작하는 것이 맞다.**
없는 축을 있는 척 채우지 않는다.

---

# 관계 엔진 (`relation_engine.py`)

증거 축 **25%** 를 배정받았지만 코드가 없던 자리를 채운다.

방법론은 새로 만든 게 아니다. `advanced analysis methods` 문서의
**"다인물 관계 심화 — 음양 방향성 분석"** 5단계를 그대로 옮겼다.

| 단계 | 검사 | 산출 |
|---|---|---|
| 1 | 양일간 vs 음일간 | 다르면 `보완적`, 같으면 `경쟁적` |
| 2 | 천간 5합 성립 여부 | 합이면 `천간합`, 극 관계였다면 `극중생합` |
| 3 | 음양 방향성 | 양이 밀고 음이 받는다 — 누가 어느 쪽인가 |
| 4 | 서로에게 어떤 십성인가 | `비대칭` / `대칭` |
| 5 | 대운 교차점 | `결실기있음` + 연도, 대운 없으면 `결실기미상` |

## 이 엔진이 하지 않는 것 — 성격을 말하지 않는다

`behavior_mapper.py` 의 첫 원칙이 여기에도 적용된다.
**"목이 많으면 성장형이다" 같은 직접 매핑은 금지다.**

그래서 출력이 자유 문장이 아니라 **정해진 태그 상수**뿐이다.

```python
TAGS = {"보완적","경쟁적","천간합","극중생합","상생","상극","동기",
        "대칭","비대칭","결실기있음","결실기미상"}
```

목록 밖의 말이 태그에 들어오면 `_assert_tags()` 가 예외를 던진다.
`"리더형"`, `"성장형"` 같은 단정은 코드가 막는다.
구조만 내놓고, 그것이 사람에게 무엇을 뜻하는지는 해석 계층이 맡는다.

## 모르는 것은 모른다고 한다

`Person.daeun` 이 `None` 이면 5단계를 **계산하지 않는다.**
`Step.known=False` 로 표시하고 `missing` 에 남긴다.

`None`(모른다)과 `{}`(대운이 없다)는 다른 상태다. 섞지 않는다.
결실기가 실제로 안 겹치면 `timing=None` 이되 `결실기미상` 태그는 붙지 않는다 —
**"모른다"와 "없다"를 구분한다.**

## ⚠️ 문서와 계산이 갈리는 지점 — 십성 편/정

문서의 사례는 이렇게 적혀 있다.

> 십성: 시저님에게 여성은 **편관**, 여성에게 시저님은 **편재** (비대칭)

이 엔진이 계산한 값은 다르다.

| | 문서 | 이 엔진 | 근거 |
|---|---|---|---|
| 己(음토) → 甲(양목) | 편관 | **정관** | 목극토=관살. 음양 **다름** → 정(正) |
| 甲(양목) → 己(음토) | 편재 | **정재** | 목극토=재. 음양 **다름** → 정(正) |

명리 표준 규칙은 **음양이 같으면 편(偏), 다르면 정(正)** 이다.
己는 음, 甲은 양이라 둘 다 정(正)이 된다.

**비대칭이라는 결론 자체는 문서와 같다** (관 vs 재). 갈리는 건 편/정 표기뿐이다.

이 엔진은 표준 규칙으로 구현했고, 문서를 조용히 고치지 않았다.
**시저님 판단이 필요하다** — 문서 쪽 표기를 쓰는 별도 유파가 있다면 알려주시면 맞춘다.
`_TEN_GOD_PAIRS` 한 곳만 바꾸면 된다.

## 연결

```python
from relation_engine import Person, analyze

rel = analyze(Person("시저", 명식_a["day_gan"], daeun=대운_a),
              Person("상대", 명식_b["day_gan"], daeun=대운_b))

# 가중치 축에 넣을 때
available = {"행동", "맥락", "타고난지표"}
if rel.timing is not None or not rel.missing:
    available.add("관계")        # 관계 데이터가 실제로 있을 때만 축을 켠다
```

`saju_engine` 의 대운 산출을 `{연도: 오행}` 으로 바꾸는 어댑터가 필요하다.
**그 형태를 확인하기 전에는 `daeun=None` 으로 두고 5단계를 비워둔다.**

검증: `python3 test_relation_engine.py` — 29/29 통과해야 한다.
