# 28_path_통계_붙였다 (2026-09-14, 안토니우스 설계 승인 e17aa3f)

## 구현물
- `/opt/data/scripts/saengui_stats.py` — 통계 모듈 (append_pan·count_paths·context_reachable·check_structural_impossibility·cross_check)
- `saengui_web.py` 패치 — /read 저장 직후 append_pan 호출 (kind="실사용"). 통계 실패는 stderr에 소리를 내되 판독을 죽이지 않음. sys.path에 /opt/data 추가.
- 통계 파일: `/opt/data/saengui_stats/YYYY-MM-DD.jsonl` — 행 1개=판 1개, 필드 {date, hexagram, path, kind}. 시각 없음.

## 설계 반영 (안토니우스 셋)
- ㈀ **12종 집합을 글자로 박지 않는다** — context_reachable()이 MATCHING_RULES(도달 가능 키만: 만개기·풍성기·험난기·완성기·시작기·기다림·화합·덜어내기) + 코드 폴백 {목51·화30·토2·금1·수29}에서 매번 계산. 유령 키 '풀림'·'단절'은 live_keys에서 제외.
- ㈁ **시험 C 재정의** — count_paths는 그날 파일이 없으면 None("없음"=수집 전)을 돌려준다. 0이 아니다. 수집이 죽은 날도 조용히 0이 되지 않는다.
- ㈂ **문장 교정** — 별도 파일의 진짜 이유는 프라이버시가 아니라 **/forget에 표본이 사라지지 않게** 하는 것이다. saju_records에는 시각이 있으므로 (날짜,괘)↔(시각,괘)를 맞추면 path가 별명에 붙을 수 있다 — 읽는 자가 이미 saju_records를 직접 보므로 새 노출은 아니나, "분리했다"고 쓰지 않는다 (E-528).
- count_paths(date, kind) — **kind 필수 인자, 기본값 금지** (E-488 같은 자리).
- 시험판 구분: kind="시험" / "시험-모순주입" — JSON 주석이 없으므로 필드로 남긴다. 지우지 않는다.

## 시험 결과 (전부 실측 값)
- 12종 집합(매번 계산): {1,2,3,5,11,24,29,30,41,51,55,63} — **안토니우스 실측 12종과 완전 일치 (차이 없음)**
- 시험 A (양방향): 1956-08-18 여 → 괘4 path=조합 ✔ / 1955-03-27 남 → 괘2 path=컨텍스트 ✔
- 시험 B-1 (합산 검산): 개별 판 합 = count_paths 결과 일치 True
- 시험 B-2 (구조 불가능): 정상 판 경보 0
- 시험 B-3 (모순 주입): {path=컨텍스트, 괘4} 가짜 판 → 경보 1건, **잡힘 True**. 가짜 판은 kind="시험-모순주입"으로 흔적 남김. **이 검사는 절반만 잡는다 — path=조합은 41괘 어느 것도 낼 수 있어 반증 불가.**
- 시험 C: 2020-01-01(수집 전) → None ("없음", 0이 아님)

## 도중에 잡은 결함 (오탐)
- 첫 계산에서 괘 2(곤위지)를 "불가능"으로 오탐했다. 원인 둘: ① 규칙 문자열의 "또는 42" 후보까지 읽었으나 실제 코드는 split("_")[0] 첫 번호만 씀 ② 코드 폴백은 primary_element_to_trigram이 아니라 default_map 하드코딩 {목51·화30·토2·금1·수29}. **검사는 규칙 파일이 아니라 실제 코드 경로가 내는 것과 대조해야 한다.** 교정 후 12종 일치.
- 이 오탐이 안 잡혔으면 실제 서비스 판(곤위지 폴백)이 매일 경보를 냈을 것이다.

## 재기동
- 이 변경은 재기동이 필요하다 (띄워진 프로세스는 옛 코드). 재기동은 시저님 자리 — 급하지 않다.

## 시각 문장 (E-528 — 설계 문서 그대로)
> 통계 파일에는 시각이 없다. 웹 로그(saengui_web.log)와 행 순서로 맞추면 복원된다.
> 한 파일에서 지운 값이 다른 파일과 맞추면 돌아온다. 없는 것이 아니라 복원 가능한 것이다.

## 240판 생성식 (안토니우스 ② 요청 — 수를 낳은 식. 검산용 정본)

```python
start = datetime(1955, 1, 1, 12, 0)
for i in range(240):
    birth = start + timedelta(days=17 * i)        # 1955-01-01부터 17일 간격 240판 (1955~1966년생)
    gender = "male" if i % 2 == 0 else "female"   # ★ 남자 먼저 교대 — 짝수 i=male
    saju = compute_saju(birth.strftime("%Y-%m-%d"), "12:00", "광주", gender)
    # 고정: birthtime 12:00 · city 광주 · 17일 간격
    # 돌림: 생년월일(17일 간격) · 성별(교대)
```

- **성별 순서가 결과를 바꾼다**: 남먼저(정본) 200/240 · 여먼저 195/240 — 성별이 대운 방향(순/역행)을 갈라 표본 자체가 달라진다. 어제(9/14) 200/40은 남먼저 판. 두 셈 다 옳다 — 표본이 다른 것이다.
- 240판 결과 JSON: saengui/shake_240_0914_result.json (요약만 — 판별 목록 없음, 재현은 위 생성식으로).

## 시험 D (안토니우스 ③ — 통계 쓰기 실패의 진짜 반증)
- 대조: saju_records 그날 판 수 ↔ 통계 파일 그날 실사용 행 수 — **두 파일이 독립이라 진짜 반증** (B-1은 같은 파일 안 일치만 봐서 append_pan 실패를 못 잡는다)
- 실측(오늘): {saju_records: 2, stats_실사용: 0, 일치: False} — **재기동 전이라 수집이 안 된다는 참을 잡았다** (오탐 아님 — 시험 D의 힘)
- 주입 시험: records 0 vs 행 2 (인위) → 불일치 → 잡힘 True. 시험용 파일(2099-01-01)은 제거 — 실사용 날짜 아님.
- cross_check() 함수 saengui_stats.py에 추가. 재기동 후 매일 이 값이 True여야 한다.
- 백그라운드 240판(여먼저 표본) 결과: 195/45 · 구조 불가능 경보 0 — 경보 검사는 표본과 무관하게 유효.
