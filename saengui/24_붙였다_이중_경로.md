# 붙였다 — 이중 경로 (2026-09-14 밤)

시저님 Go. 조합 경로를 서비스 엔진에 붙였다.

## 변경 (saengui-munui/backend/engines/saju_interpreter.py)
- 449행: match_hexagram → _match_hexagram_dual (호출 1곳만)
- 조합 먼저 → covered면 그 괘 + path="조합" / None이면 기존 컨텍스트 + path="컨텍스트"
- 여섯 참/거짓: saju_lines.py가 saju 프로필에서 읽음 (새 계산 0)
- available: iching_data.json 파일에서 읽음 (손으로 적지 않는다)
- narrative도 새 괘로 재생성 — 옛 괘 서사 잔류 결함 발견·수정

## 게이트 결과 (가상 명식)
- A(1956-08-18 여): 전 30 리(폴백) → 후 **4 몽 艮/坎 path=조합** ✔️ — 컨텍스트 12종에 없는 괘
- B(1955-03-27 남): 전 2 곤 → 후 **2 곤 path=컨텍스트** ✔️ — 폴백 판 불변 (더하는 변경 확인)
- 240판 전체: 조합 200 / 컨텍스트 40 · 조합 괘 trigrams ↔️ 표 어긋남 0
- 데이터 복구 사고: 삽입 스크립트가 meta·trigrams·matching_rules를 날림 → 백업에서 복구·스크립트 수정(구조 보존)

## 8870 상태
- 재시작 금지(감시 밖) 준수. 실측: 현재 프로세스 안 떠 있음(수동 서비스).
  다음 기동 시 변경 반영됨 — 기동은 시저님/바깥 결정.

## 8870 기동 결과 (2026-09-14 아침, 시저님 "오늘 올린다")
- 기동 명령은 state.db 과거 세션(실측 실행 원문)에서 발견:
  `cd /opt/data && nohup /opt/data/vis_env/bin/python saengui_web.py > /opt/data/saengui_web.log 2>&1 &`
- detached 기동: proc_91c9c111c0c8 (PID 114943) · 로그=/opt/data/saengui_web.log
- 게이트: ㈀ HTTP 200 ㈁ 최상위 키 4개(meta·trigrams·hexagrams·matching_rules)·hexagrams 41·trigrams 8 ㈂ 웹앱 POST로 4 몽 산하수몽 화면 등장(조합 경로 증거 — 4번은 컨텍스트 12종에 없음) · 반대시험 2 곤위지(폴백 불변)
- 기록 파일(saju_records)에는 괘 번호만 저장(4/2) — path는 데이터에는 있으나 화면·기록에 미표시 (별도 결정 대기)
- 푸시: 실패 — 자격증명 부재. 로컬 git은 origin과 영영 갈린다(E-511). 남길 것은 블록으로 보낸다.
