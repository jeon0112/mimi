# 조합 경로가 섰다 — **64가지가 64로 간다**

> 2026-09-14 · 시저님 결정 ㈀(E-432). **원장 E-443 ~ E-449.**
> 되살린 대상: 2026-09-11 의 `_shake18.py` — **측정 후 삭제되어 코드가 없었다**(E-425).

---

## 0. ★★★★★ 먼저 — **원장을 쳤더니 이미 다 있었다**

어제 만든 스킬 `원장-먼저` 를 **처음 쓴 자리가 여기다.**

```
grep -n "효 배치\|초효\|8괘" proposals/LEDGER.md
   →  ★ E-132 · E-133 · E-134 · E-135 가 한꺼번에 나왔다
```

| | |
|---|---|
| **E-133** | ★★★★★ **시저님 결정 ⑩ — 효 여섯 확정.** 하괘 초효 일간음양 · 2효 신강신약 · 3효 월지계절 / 상괘 4효 대운순역 · 5효 천을귀인 · 상효 대운지지음양 |
| **E-132** | 배치 근거 — **5효는 임금의 자리**라 천을귀인 · 2효는 내괘의 중이라 신강신약 · 3효는 안팎의 경계라 월지 |
| **E-134** | **여섯 다 「채워지고 나아가는 쪽」이 양**이다. 억지로 맞춘 자리가 없다 |
| **E-135** | ★★ *"`trigrams` 키에서 **역으로 매핑을 뽑을 수 있다**"* |

> ## ★★★★★ **어제 내가 뭉클이에게 물어서 받은 것이, 원장에 이미 있었다.**
> **스킬이 만들어진 다음 날 값을 했다.**

---

## 1. 어디까지 내가 쓸 수 있나 — **엔진은 VPS 에 있다**

```
saengui/ 에 사주 엔진이 없다 · iching_data.json 도 없다   ★ 둘 다 VPS 에만 있다
```

★ 그런데 **어제 손금에서 한 것과 같은 길이 있다** —

```
명식·대운  →  ★ 효 6개(이진)  →  8괘  →  상/하 조합  →  괘 번호
   ↑ VPS 가 한다        ↑★★★★★ 여기부터가 순수 함수다. 여기서 쓰고 여기서 시험한다
```

★★ `palm_features.py` 가 **사진을 안 보듯**, `hexagram_from_saju.py` 는 **사주를 안 본다.**
★★★ **엔진을 들고 오지 않아도 변환은 전부 검증된다.** 그래서 오늘 안에 끝났다.

---

## 2. ★★★★★ 가장 센 검증 — **64가지가 64로 간다**

효 여섯이 전부 이진이니 입력은 **2⁶ = 64가지**다. 그리고 —

```
★★★ 64가지 입력  →  64가지 조합  →  64개 괘 번호      겹침 0
```

> ## ★★★★★ **효 배치가 완전한 일대일이다. 낭비가 하나도 없다.**

★ 하나라도 겹치면 **두 사람이 다른 명식인데 같은 괘를 받는다.** 그런 자리가 없다.
★★ 이것은 **E-134**(여섯 다 자연스럽다)를 수치로 다시 확인한 셈이다.

---

## 3. **64괘 표를 어떻게 믿는가** — 기억에서 쓰면 틀린다

★ 어제 배운 것이다 — **예언은 단어가 아니라 어미로 온다**(E-421)를 찾을 때,
**목록을 손으로 늘리는 방식이 구멍을 못 막는다**는 것을 봤다.

```
표를 쓴다            →  ★ 그리고 「아는 것」으로 자가 검증한다
KNOWN 37개          →  뭉클이가 데이터에서 원문으로 옮긴 것만
import 시점에 대조   →  ★★ 어긋나면 바로 죽는다
```

> ## ★★★★ **틀린 표로 조용히 도는 것보다 죽는 편이 낫다.**
> **매칭이 `trigrams` 를 안 읽어서 「틀려도 에러가 안 나던 것」이 어제의 문제였다**(E-374).

★ **음성 대조로 확인했다** — 표의 5번을 일부러 망가뜨리니 **죽는다.**

---

## 4. 없는 괘는 **없다고 한다**

```
巽/兌 (61 中孚)  →  조합은 나온다  →  ★ number = None
```

★★ **가까운 괘로 대신하지 않는다.** 메우면 덮임 숫자가 거짓말이 된다.
★★★ **「덮임」의 정의가 그 `None` 의 비율이다.**

---

## 5. 덮임 — **두 가지 수를 섞지 않는다**

| | 무엇을 나눈 값 | 24괘 | **37괘** |
|---|---|---:|---:|
| **조합 기준** | 64 조합 중 글이 있는 것 | 37.5% | ★ **57.8%** |
| **사람 기준** | 240판 중 덮인 판 (E-409) | **42.5%** | ★★ **아직 모른다** |

★★★★★ **둘은 다른 수다.** 24괘 때 조합 37.5% 인데 사람 42.5% 였던 이유는 —
**흔한 조합이 덮여 있었기 때문**이다(E-148: *"상위 32 조합이 86.2%"*).

> ## ★★★ **그러면 37괘의 사람 기준 덮임은 57.8% 보다 높을 것이다.**
> **흔한 것부터 채웠으니까. 그러나 「이론상」이고, 240판을 다시 돌려야 실측이 된다.**

★ **지어내지 않는다.** 재실측 전까지는 **조합 기준 57.8%** 만 말한다.

---

## 6. 남은 것 — **붙이는 일은 VPS 몫이다**

```
엔진이 낼 것 여섯 (전부 참/거짓)
  ilgan_yang         일간이 갑병무경임인가
  singang            신강인가
  month_warm         월지가 봄여름인가
  daeun_forward      대운이 순행인가
  cheoneul           천을귀인이 드는가
  daeun_branch_yang  대운 지지가 자인진오신술인가
```

★★ 이 여섯만 넘기면 **괘 번호가 나온다.** 그다음 `iching_data.json` 에서 글을 꺼낸다.
★★★ 그리고 **컨텍스트 경로와의 관계**를 정해야 한다 — 덮는가, 보조인가.
★ 조합이 `None` 일 때 **컨텍스트가 받는 것**이 자연스럽다. 그러면 둘이 안 부딪힌다.
