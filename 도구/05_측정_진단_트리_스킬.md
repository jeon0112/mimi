# 측정 진단 트리 — 뭉클이 스킬 전문과 검토

> 2026-09-18 · 뭉클이 작성(SKILL.md 86행, 5,326바이트) + 안토니우스 검토
> **정본 = `/opt/data/skills/software-development/port-firewall-diagnosis/SKILL.md` (VPS)**
> **이 파일은 사본이다. 실행되는 것이 정본이다.** (E-627)
>
> ✓ **2번 들여쓰기는 전달 중에 깨진 것이었고, 내 복원이 정본과 일치함을 대조로 확인했다** (E-633). **사본 = 정본.**

---

## 1. 뭉클이 최종본 (2026-09-18 22:5x)

```
---
name: port-firewall-diagnosis
description: Use when something doesn't work and the cause is unknown — 안 열린다, 안 된다, 막혔다, 접속이 안 된다, 됐나, 열렸나, 왜 안 되지.
---

# 측정 진단 트리 — "됐나 안 됐나"를 재는 법 (2026-09-18 실측으로 완성)

포트·방화벽은 이 스킬의 한 경우다. DNS·HTTP·파일 존재·서비스 상태 —
"됐나 안 됐나"를 재는 모든 자리에 같은 트리가 걸린다.

오류 코드를 끝까지 읽어라 — 실패 화면은 실패만 말하지 않는다.
- ERR_NAME_NOT_RESOLVED → 이름이 안 풀림 (DNS 문제)
- ERR_CONNECTION_TIMED_OUT → 이름은 풀렸고 TCP가 답이 없음 (방화벽/경로 문제)
- ERR_CONNECTION_REFUSED → 도달했으나 포트가 닫힘 (리스너 없음)

## 진단 순서 (반드시 이 순서)

1. **가장 안쪽에서부터, 싼 것부터 잰다** (재는 것은 공짜, 여는 것은 되돌리기 어렵다)
   원리는 하나다 — 안에서 밖으로.
   - 포트/서비스면: `curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:<port>/`
     그리고 `ss -ltnp | grep <port>` (또는 /proc/net/tcp). 꺼져 있으면 방화벽을 열어도 안 열린다.
   - DNS면: 로컬 리졸버·DoH(dns.google) 순으로 질의.
   - 파일이면: 그 자리에서 ls·read_file.
   - 어떤 경우든: 대상이 살아있는지부터. 헛수고 전에 이것부터.

2. **내 관측점이 '바깥'인지 검증** (헤어핀 함정 — 9/18 사고)
   curl -o /dev/null -w '%{http_code} %{time_connect}s\n' http://<공인IP>:<port>/
   time_connect가 0.00X초 = 같은 호스트 안에서 돌아온 것. 바깥 왕복은 최소 수십 ms.
   대조: google.com 등 바깥 RTT와 비교 (예: 216ms).
   **컨테이너→자기 호스트 공인IP는 방화벽 인바운드를 우회한다. "외부 200"이라 쓰지 마라.**

3. **진짜 바깥 대조군 2축**
   - 같은 서버 다른 포트 (예: 8826 vs 8870) — 바깥 관측점에서 둘 다 침. 하나만 막히면 방화벽 포트 규칙.
   - 다른 네트워크 축 (PC vs 휴대폰 LTE) — 둘 다 안 되면 서버 쪽, 하나만 안 되면 그 관측점 망의 아웃바운드 차단(비표준 포트는 ISP/사내망이 막는 경우 실재).

4. **이력 대조 (꿈의 단계 — 재실행 전에)**
   과거 기록과 정면 어긋나면 원인을 고르기 전에 모순을 먼저 풀어라.
   예: "9/14 외부 200" vs "오늘 타임아웃" → 9/14 측정이 헤어핀이었다(관측점 결함)로 해소.

## 원인 후보 3형태 (모순 풀 때 사용)

- ㈎ 과거 측정의 관측점이 바깥이 아니었다 (제일 흔함 — 헤어핀)
- ㈏ 그 사이 방화벽이 바뀌었다
- ㈐ 막힌 게 방화벽이 아니다 (아웃바운드 차단·리스너 사망 등)

## 우리 환경 사실 (2026-09-18 기준)

**이 절은 썩는다. 쓰기 전에 다시 잰다.** — 방화벽을 열면 바뀌고, 터널을 세우면 바뀐다.
(절차는 안 썩는다. 환경 사실은 관측이라 썩는다 — ↯ 절과 같은 원리.)

- Hostinger VPS 145.79.8.136. 컨테이너 egress: 443/tcp 됨, 임의 IP·포트 불가(안토니우스 컨테이너도 동일).
- 8870 = 인생지도(saengui_web, 재부팅 시 수동 실행). 8826 = 포스팅고.
- Hostinger 방화벽 인바운드 여닫음 = 시저님 자리 (뭉클이 권한 없음).
- 대안: Cloudflare Tunnel(cloudflared, /opt/data/bin/) — 인바운드 0개·전구간 암호화.
  egress 7844/UDP+TCP 필요(미측정), 폴백 --protocol http2.

## 관측 조건 칸 (필수 — 2026-09-18 안토니우스 입법)

"열렸다·닫혔다·있다·없다"를 적을 때 값과 함께 셋을 적는다:
- **어디서 쟀나** (관측점 — 컨테이너 안/헤어핀/바깥 누구의 자리)
- **대조군은 무엇이었나**
- **걸린 시간** (time_connect — 0.00X초면 헤어핀, 그날 무효 판정 가능)

실례: E-554 "바깥에서 200"이 나흘을 산 건 이 칸이 없었기 때문이다.

## 무효화 표식 (↯)

관측 노드는 관측 조건에 오염되어 나중에 죽는다(구글의 실행 결과 트리와 다른 점).
- 무효가 드러난 행은 **지우지 않고** ↯ 표식 + 회수 사유 + 참조를 그 행에 직접 단다.
- 원문 한 글자도 안 지운다 — 지우면 왜 틀렸는지가 사라진다.
- 새 행 정정만으로는 부족하다: 원문을 읽는 사람은 뒤의 정정행을 못 본다.

## 수 옮기기 규칙 (2026-09-18 전사 오류 사고)

- 배율이 적힌 수를 옮기면 **두 수를 직접 나눠 본다** (550/317=1.74배 ≠ 162배 → 걸림).
- 도메인이 다른 지표를 한 괄호에 묶지 않는다 (호출 수 162배 / 실행시간 1.22배는 서로 다른 지표다).
- 합계·배율은 제 부품과 대조한다.

## 규율

- 방화벽을 열기 전에 평문 노출 여부를 시저님께 고지하고 결정받는다 ("여시면 오늘부터 평문입니다").
- 크론은 죽음을 보고만 하고 되살리지 않는다 (재는 것과 고치는 것 분리).
- 측정 전 대조군 필수 — 대조군을 안 돌리면 틀린 값이 맞는 값을 끌어내린다.
- 기억·사전지식은 처방이 아니라 물음으로 낸다 ("이건 내 기억이다, 재라") — 트리 바깥을 꿀 때의 형식.
```

---

## 2. 최종본에서 고쳐진 넷 (판정: 통과)

| | 내가 권한 것 | 최종본 |
|---|---|---|
| ① | `description` 에 한국어 트리거 | **탑재** — 안 열린다·안 된다·막혔다·접속이 안 된다·됐나·열렸나·왜 안 되지 |
| ② | `anything` → 「원인을 모를 때」 | **좁힘** — *something doesn't work and **the cause is unknown*** |
| ③ | 1단계 일반화 | **「안에서 밖으로」 + 세 갈래**(포트·DNS·파일) |
| ④ | 「이 절은 썩는다」 | **박음** — **그리고 이유까지 적었다** |

**④ 에 내가 안 준 한 줄이 들어왔다:**

> *(절차는 안 썩는다. 환경 사실은 관측이라 썩는다 — ↯ 절과 같은 원리.)*

**규칙을 적은 것이 아니라 왜 그 규칙인지를 적었다.** 옆 절과 묶었다.

**③ 에도 내가 안 준 값이 있다** — `DNS면: 로컬 리졸버·DoH(dns.google) 순으로 질의`.
**오늘 내가 DoH 로 실패하고 대조군으로 겨우 잡은 그 경로다**(E-589).

---

## 3. 조건 셋 (E-611) 최종 확인

- ㈀ **스킬 하나** — 늘리지 않았다. 이름도 유지(경로가 바뀌면 위험이 붙는다)
- ㈁ **관측 조건 칸 「필수」** — E-554 실례까지
- ㈂ **무효화 표식 ↯** — 원문 보존 + 새 행 정정만으로 부족한 이유까지

---

## 4. ⚠ 내가 손댄 한 곳 — 밝힌다

**전달 과정에서 2번 항목의 `curl` 줄이 별도 코드 블록으로 빠져나와 들여쓰기가 깨졌다.**
**위 전문에서는 원래 자리(들여쓴 본문)로 되돌려 놓았다.**

- **내용은 한 글자도 안 바꿨다.** 바꾼 것은 줄의 들여쓰기뿐이다.
- **깨진 것이 VPS 원본인지 전달 경로인지는 내가 모른다** — 뭉클이는 `read_file` 실측본이라 했으므로 경로일 가능성이 높다.
- **그래도 적는다. 사본에 손을 대면 사본이 정본과 달라지기 때문이다.**

**오늘 뭉클이가 수를 옮기다 자릿수를 틀렸고**(E-604)**, 이번에는 형식이 깨졌다.
둘 다 「옮기는 중에 바뀐다」는 같은 계보다.** (E-632)

**✓ 닫혔다 (E-633).** 뭉클이가 VPS 정본 26~32행을 다시 읽어 대조했다 —
**코드 블록은 2번 항목 안에 3칸 들여쓰기로 제자리에 있었다. 깨진 것은 전달 경로였다.**

> **내가 조용히 고쳤으면 이 물음은 열리지도 않았고, 사본이 정본 행세를 시작했을 것이다.
> 밝히는 것이 값을 했다.**
